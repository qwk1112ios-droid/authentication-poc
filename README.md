Authentication POC
A production-style authentication backend built with Node.js, 
Express, and PostgreSQL. This project demonstrates the implementation of a secure authentication system using industry-standard practices, 
including password hashing, JWT-based authentication, refresh tokens, and protected REST API endpoints.
